<html lang="en">

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="/docs/4.0/assets/img/favicons/favicon.ico">

    <title>Bienvenue <%= params[:user].first_name %> ! </title>

    <link rel="canonical" href="https://getbootstrap.com/docs/4.0/examples/album/">

    <!-- Bootstrap core CSS -->
    <link href="../../dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="album.css" rel="stylesheet">
  </head>



      <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">Bienvenue <%= params[:user].first_name %> !</h1>
          <p class="lead text-muted">Ici, c'est notre super site de potins, il est chouette nan ??? <br/>
          Tu peux voir  ci-dessous la liste des potins que tu as déjà crée !</p>
          <p>
            <a href="#" class="btn btn-primary my-2">Tu veux modifier un potin?</a>
            <a href="#" class="btn btn-secondary my-2">Tu veux en créer un nouveau?</a>
          </p>
        </div>
      </section>


    <p> <% params[:user].gossips.each do |gossip|  %> </p>
     <div class="album py-5 bg-light">
        <div class="container">

          <div class="row">
            <div class="col-md-4">
              <div class="card mb-4 box-shadow">
                <div class="card-body">
                  <p class="card-text"><%= gossip.title %> by <%= gossip.user.first_name %></p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                      <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                    </div>
                    <small class="text-muted"><%= gossip.created_at %> </small>
                  </div>
                </div>
              </div>
            </div>
        </div>
      </div>
    </div>
      <% end %>


  </html>
